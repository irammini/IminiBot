# .env - Biến môi trường cho ứng dụng IminiBot
#
# File này chứa các cấu hình quan trọng cho IminiBot.
# Vui lòng điền đầy đủ các giá trị cần thiết trước khi khởi chạy ứng dụng.

# 🔐 Cấu hình Discord Bot
# -----------------------------------------------------------------------------
# BOT_TOKEN (BẮT BUỘC): Token xác thực của bot Discord của bạn.
# Bạn có thể lấy token này từ Discord Developer Portal.
BOT_TOKEN=

# 🛢️ Cấu hình Cơ sở dữ liệu PostgreSQL
# -----------------------------------------------------------------------------
# DATABASE_URL (BẮT BUỘC): URL kết nối đến cơ sở dữ liệu PostgreSQL.
# Cấu trúc: postgresql+asyncpg://<username>:<password>@<host>:<port>/<database_name>
# Ví dụ: postgresql+asyncpg://iminibot:password@localhost:5432/iminibot
DATABASE_URL=

# 🎮 Cấu hình Hệ thống XP và Level-up
# -----------------------------------------------------------------------------
# Các biến này được sử dụng trong các lệnh như 'daily' và sự kiện 'on_message'
# để tính toán và quản lý điểm kinh nghiệm (XP) của người dùng.
XP_BASE=100
XP_POWER=1.2

# 🔁 Cấu hình Thời gian Cooldown
# -----------------------------------------------------------------------------
# Các biến này định nghĩa thời gian chờ (cooldown) cho các lệnh cụ thể, tính bằng giây.
DAILY_COOLDOWN=86400           # giây — 24h
DEPOSIT_COOLDOWN=1200          # giây — 20 phút

# 🌟 Cấu hình ID Kênh quan trọng
# -----------------------------------------------------------------------------
# Các biến này lưu trữ ID của các kênh Discord cụ thể để bot tương tác.
LEVEL_UP_CHANNEL_ID=0 # kênh thông báo lên cấp
LOG_CHANNEL_ID=0    # (tuỳ chọn) kênh ghi audit log